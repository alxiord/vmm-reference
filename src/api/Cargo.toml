[package]
name = "api"
version = "0.1.0"
authors = ["rust-vmm AWS maintainers <rust-vmm-maintainers@amazon.com>"]
edition = "2021"

[dependencies]
clap = "4.5.7"

vmm = { path = "../vmm" }

[dev-dependencies]
linux-loader = "0.4.0"
